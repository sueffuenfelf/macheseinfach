/** Shared theme for Zeit & Fristen tools — matches area `zeit` accent. */
export const ZEIT_THEME = {
    accent: '#ffb347',
    accentStrong: '#000',
    accentSoft: '#fff0d6',
} as const;

export const TRUST_LOCAL = 'Lokal berechnet · nichts wird hochgeladen';
