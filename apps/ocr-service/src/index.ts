/**
 * OCR service stub — replace with Gemma/local model pipeline.
 */
const port = Number(process.env.PORT ?? 8787);

console.log(`[ocr-service] listening stub on :${port} (not implemented yet)`);

export {};
