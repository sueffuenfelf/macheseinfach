# Cursor agents (stub)

Add agent profiles here as the project grows. Start with a single implementer:

## `implement.md`

- Read nearest `AGENTS.md`
- Prefer `@macheseinfach/ui` components
- Verify with `bun test` and `bun run lint`
